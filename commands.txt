https://medium.com/@dharamai2024/automated-pr-review-agent-with-langchain-langgraph-058003653e3e

MONGO
	brew services start mongodb/brew/mongodb-community
Mysql
	http://127.0.0.1:5500/index.html
	quit to exit
CONDA
Conda create -p venv python==3.10
Conda activate venv
Common conda Commands
Environment Management:
conda create -n <env_name> <package_list>: Creates a new environment named <env_name> and installs specified packages.
conda activate <env_name>: Activates the specified environment.
conda deactivate: Deactivates the current active environment.
conda env list: Lists all available environments.
conda env remove -n <env_name>: Removes the specified environment.
Package Management:
conda install <package_name>: Installs a package into the active environment.
conda update <package_name>: Updates a specific package.
conda update --all: Updates all packages in the active environment.
conda remove <package_name>: Removes a package from the active environment.
conda list: Lists all installed packages in the active environment.
Other Useful Commands:
conda info: Provides information about the current conda installation.
conda config --show: Displays current conda configuration settings.
conda config --set <key> <value>: Sets a specific configuration value.
conda clean --all: Removes unused packages and caches.
Remember:
To install a specific version of a package, use the == operator: conda install <package_name>=<version>
To specify a channel for a package, use the -c flag: conda install -c <channel_name> <package_name>
For more advanced usage and troubleshooting, refer to the official conda documentation.

PYTHON
	Pip install ipykernel (to run ipynb files on vs code)
!pip install pandas (you can install in a separate cell)but not ideal

VS-CODE
	Cmd + shift + p (to open the palette)

PYTHON
Python -m virtualenv venv
source .venv/bin/activate # to activate a environment
deactivate # to deactivate the environment

python manage.py runserver 
pip install -r requirements.txt
django-admin 
django-admin startproject devsearch 
python manage.py runserver
control c # to stop thew server
python3 manage.py startapp projects# create a new app
-------
super user name rahulalladi
mail bunnyfeb10@gmail.com
pwd R@hul123
admin panel will only be available if initial migrations are made
--------
ython manage.py shell
if u want to create a model create a class and inherit Django model
ex:- class Project(models.Model):
        title = models.CharField()
        description = 
        id = models.UUIDField()
--------
test2 = Project.objects.filter(vote_ratio__gte=50)
                or
        Project.objects.all()
                or        
        ecommerce_website = Project.objects.get(title='Ecommerce Website')
        print(ecommerce_website.review_set.all()) # this will print all the reviews of ecom project notive reviews is small case even though model name is Review(you have to give in small case only)
print(test2)

project = Project.objects.get(title='Ecommerce Website')
print(project.tags.all()) #to get all the tags of ecom website
--------
--do this if u want to pull develop changes to your feature branch and push the changes of your branch--
git checkout develop
git pull
git checkout feature/name
git rebase develop
if u get any conflicts resolve them
git add . the changes if u resolved any conflicts
git rebase --continue
git push --force origin feature/name


